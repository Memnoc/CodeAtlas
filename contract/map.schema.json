{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "KnowledgeGraph",
  "type": "object",
  "properties": {
    "domain_flows": {
      "description": "Mechanically projected domain flows. Optional in the contract (older\nmaps omit it); always emitted by the CLI.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/DomainFlow"
      }
    },
    "edges": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Edge"
      }
    },
    "layers": {
      "description": "Directory-derived layers every file node is assigned to. Optional in\nthe contract (older maps omit it); always emitted by the CLI.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/Layer"
      }
    },
    "nodes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Node"
      }
    },
    "project": {
      "$ref": "#/$defs/Project"
    },
    "tour": {
      "description": "The guided tour: an ordered walk over the file nodes. Optional in the\ncontract (older maps omit it); always emitted by the CLI.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/TourStep"
      }
    },
    "version": {
      "description": "Semver version of the map contract this file conforms to.",
      "type": "string"
    }
  },
  "required": [
    "version",
    "project",
    "nodes",
    "edges"
  ],
  "$defs": {
    "DomainFlow": {
      "description": "A call chain rooted at an entry point — a function nothing else calls.\nThe chain and its domain are structural facts; the name is the enrichable\nslot — mechanically it renders the chain, enrichment may relabel it.",
      "type": "object",
      "properties": {
        "domain": {
          "description": "The domain this flow belongs to: the top-level directory of the root\nfunction's file, or `root` for files at the repository root.",
          "type": "string"
        },
        "id": {
          "description": "Stable flow ID derived from the root function's node ID, e.g.\n`flow:function:src/main.ts:main`.",
          "type": "string"
        },
        "name": {
          "description": "Mechanical or enriched display name; provenance says which.",
          "type": "string"
        },
        "provenance": {
          "$ref": "#/$defs/Provenance"
        },
        "steps": {
          "description": "Function node IDs along the chain, root first, in deterministic\ndepth-first call order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/NodeId"
          }
        }
      },
      "required": [
        "id",
        "name",
        "domain",
        "steps",
        "provenance"
      ]
    },
    "Edge": {
      "type": "object",
      "properties": {
        "kind": {
          "$ref": "#/$defs/EdgeKind"
        },
        "source": {
          "$ref": "#/$defs/NodeId"
        },
        "target": {
          "$ref": "#/$defs/NodeId"
        },
        "weight": {
          "description": "Fixed weight determined by `kind`; see [`EdgeKind::weight`].",
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "source",
        "target",
        "kind",
        "weight"
      ]
    },
    "EdgeKind": {
      "type": "string",
      "enum": [
        "contains",
        "imports",
        "exports",
        "calls"
      ]
    },
    "Layer": {
      "description": "A horizontal grouping of files. Membership is structural (each file node's\n`layer` field); the name is the enrichable slot — mechanically it is the\nderiving directory, enrichment may relabel it.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable layer ID: the top-level directory that derived it, or `root`\nfor files at the repository root.",
          "type": "string"
        },
        "name": {
          "description": "Mechanical or enriched display name; provenance says which.",
          "type": "string"
        },
        "provenance": {
          "$ref": "#/$defs/Provenance"
        }
      },
      "required": [
        "id",
        "name",
        "provenance"
      ]
    },
    "Node": {
      "type": "object",
      "properties": {
        "id": {
          "description": "Typed ID, e.g. `file:src/main.ts` or `function:src/main.ts:main`.",
          "$ref": "#/$defs/NodeId"
        },
        "kind": {
          "$ref": "#/$defs/NodeKind"
        },
        "layer": {
          "description": "ID of the layer this file node belongs to; absent on symbol nodes,\nwhich inherit their file's layer through containment.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": "string"
        },
        "path": {
          "description": "Repo-relative path with forward slashes.",
          "type": "string"
        },
        "provenance": {
          "$ref": "#/$defs/Provenance"
        },
        "range": {
          "anyOf": [
            {
              "$ref": "#/$defs/Range"
            },
            {
              "type": "null"
            }
          ]
        },
        "summary": {
          "description": "Mechanical or enriched description; provenance says which.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "kind",
        "name",
        "path",
        "summary",
        "provenance"
      ]
    },
    "NodeId": {
      "description": "Typed node ID — the map's identity primitive. Format:\n`<kind-prefix>:<path>` for files, `<kind-prefix>:<path>:<symbol>` for\nsymbols, e.g. `file:src/main.ts` or `function:src/main.ts:main`. All ID\nminting goes through the constructors here; nothing else formats IDs.",
      "type": "string"
    },
    "NodeKind": {
      "type": "string",
      "enum": [
        "file",
        "function",
        "class"
      ]
    },
    "Project": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ]
    },
    "Provenance": {
      "description": "Whether a node's descriptive fields were produced mechanically or by LLM\nenrichment (ADR-0005).",
      "type": "string",
      "enum": [
        "structural",
        "llm"
      ]
    },
    "Range": {
      "description": "1-based inclusive line range within the node's file.",
      "type": "object",
      "properties": {
        "end_line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "start_line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "start_line",
        "end_line"
      ]
    },
    "TourStep": {
      "description": "One stop on the guided tour. The step's position comes from topology\nscoring and is structural; the label is the enrichable slot — mechanical\nby default, enrichment may narrate it.",
      "type": "object",
      "properties": {
        "label": {
          "description": "Mechanical or enriched narration; provenance says which.",
          "type": "string"
        },
        "node": {
          "description": "The node this step visits.",
          "$ref": "#/$defs/NodeId"
        },
        "provenance": {
          "$ref": "#/$defs/Provenance"
        }
      },
      "required": [
        "node",
        "label",
        "provenance"
      ]
    }
  }
}
